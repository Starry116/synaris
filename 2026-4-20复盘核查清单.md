# Synaris 项目复盘核查清单

经过逐文件扫描，以下是按优先级排列的待完成清单：

---

## 🔴 P0 — 致命缺失（会导致服务无法启动/运行）

| # | 文件 / 组件 | 问题描述 |
|---|---|---|
| ✅️1 | `app/api/chat.py` | **文件为空**，聊天核心 API 完全缺失，`/chat` 和 `/chat/stream` 端点无法使用 |
| 2 | `dependencies.py` | `knowledge.py` 和 `rag.py` 都依赖 `from dependencies import document_service_instance / vector_store_instance / rag_service_instance`，但该文件**根本不存在** |
| 3 | `core/auth.py` → `AuthMiddleware` | `main.py` 中 `app.add_middleware(AuthMiddleware)` 引用了 `AuthMiddleware`，但 `auth.py` 里**从未定义**该中间件类 |

---

## 🟠 P1 — 功能不完整（服务能启动但核心功能残缺）

| # | 文件 / 组件 | 问题描述 |
|---|---|---|
| 4 | `services/vector_store.py` → `list_documents()` | `knowledge.py` 调用了 `vector_store.list_documents(collection_name=...)` 方法，但 `VectorStoreService` 中**未实现**该方法（只有 `get_collection_stats`）|
| 5 | `migrations/versions/` | Alembic 版本目录下只有 `.gitkeep`，**没有任何实际迁移文件**，数据库无法初始化（含 `token_usage_logs` 表、所有 ORM 表）|
| 6 | `agents/tool_registry.py` → 工具命名对齐 | `setup_default_registry()` 中导入 `web_search_tool`、`rag_retrieval_tool` 等，但各工具文件中 `@tool` 装饰器生成的对象名分别是 `web_search`、`rag_retrieval`、`calculator`、`code_executor`，**命名不一致**会导致导入报错 |
| 7 | `services/document_service.py` ↔ `api/knowledge.py` 接口错配 | `knowledge.py` 调用 `doc_service.process_document(tmp_path)` 并期望返回含 `total_chunks / stored / failed / duration_ms` 的对象，但 `document_service.py` 的 `process_document()` 返回 `ProcessingReport`，需确认字段名完全对齐 |
| 8 | `main.py` → `log_registered_metrics()` 调用位置 | 在 `create_app()` 末尾直接调用 `log_registered_metrics()`，但此时 FastAPI 实例尚未启动，建议移至 `lifespan` 启动阶段 |

---

## 🟡 P2 — 配置文件空白（部署相关）

| # | 文件 | 问题描述 |
|---|---|---|
| 9 | `alembic.ini` | **空文件**，Alembic 无法读取数据库连接串，`alembic upgrade head` 会直接报错 |
| 10 | `monitoring/prometheus.yml` | **空文件**，Prometheus 无法抓取任何 Target，监控系统失效 |
| 11 | `monitoring/grafana_dashboard.json` | **空文件**，Grafana 无法导入仪表盘 |
| 12 | `nginx/nginx.conf` | **空文件**，Nginx 容器启动会报语法错误 |
| 13 | `scripts/start.sh` | **空文件**，README 中描述的一键启动命令无法执行 |
| 14 | `scripts/health_check.sh` | **空文件**，生产部署后健康检查脚本缺失 |
| 15 | `pyproject.toml` | **空文件**，项目元数据和 lint/format 工具配置缺失（black、ruff、mypy 等） |
| 16 | `synaris/README.md`（内部） | **空文件** |

---

## 🟢 P3 — 测试文件全部缺失

| # | 文件 | 问题描述 |
|---|---|---|
| 17 | `tests/conftest.py` | **空文件**，pytest fixture（Mock DB、Mock Redis、Mock OpenAI）全部缺失 |
| 18 | `tests/test_chat_api.py` | **空文件** |
| 19 | `tests/test_rag_pipeline.py` | **空文件** |
| 20 | `tests/test_agent_workflow.py` | **空文件** |
| 21 | `tests/test_multi_agent.py` | **空文件** |

---

## 🔵 P4 — 文档全部缺失

| # | 文件 | 问题描述 |
|---|---|---|
| 22 | `docs/api_reference.md` | **空文件** |
| 23 | `docs/architecture.md` | **空文件** |
| 24 | `docs/deployment.md` | **空文件** |

---

## 📊 汇总统计

```
总计缺失项：24 项
├── P0 致命缺失：   3 项（服务无法运行）
├── P1 功能残缺：   5 项（核心功能不完整）
├── P2 配置缺失：   8 项（部署相关）
├── P3 测试缺失：   5 项（质量保障）
└── P4 文档缺失：   3 项（交付物）
```

---

## 🎯 建议修复顺序

```
第一优先级（让服务跑起来）：
  P0-1 补全 api/chat.py
  P0-2 新建 dependencies.py（服务实例注入容器）
  P0-3 在 auth.py 中补充 AuthMiddleware 类
  P1-4 在 vector_store.py 中补充 list_documents() 方法
  P1-6 修正 tool_registry.py 中的工具命名对齐

第二优先级（让部署跑起来）：
  P2-9  补全 alembic.ini
  P2-12 补全 nginx/nginx.conf
  P2-13 补全 scripts/start.sh
  P1-5  生成首个 Alembic 迁移文件

第三优先级（生产就绪）：
  P2-10 补全 prometheus.yml
  P2-11 补全 grafana_dashboard.json
  P3    补全所有测试文件
  P4    补全所有文档
```

需要我从哪个优先级开始着手补全？