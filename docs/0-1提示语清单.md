🏗️ 第一阶段：基础设施（Step 1–4）

✅️**Step 1 — `config/settings.py`**

```
请实现 app/config/settings.py，要求：

1. 使用 pydantic-settings BaseSettings 管理所有环境变量
2. 配置分组（各组用独立 class 再组合）：
   - AppConfig: APP_NAME, APP_VERSION, DEBUG, API_PREFIX
   - OpenAIConfig: OPENAI_API_KEY, OPENAI_BASE_URL, DEFAULT_MODEL, FALLBACK_MODEL
   - MilvusConfig: MILVUS_HOST, MILVUS_PORT, MILVUS_COLLECTION_NAME
   - RedisConfig: REDIS_URL, REDIS_MAX_CONNECTIONS, CACHE_TTL_SHORT(10min), CACHE_TTL_LONG(24h)
   - LogConfig: LOG_LEVEL, LOG_FORMAT(json/text)
3. 支持从 .env 文件加载，附上完整的 .env.example 内容
4. 提供 get_settings() 单例函数（使用 @lru_cache）

只需输出这一个文件，包含完整代码和关键注释。
```

**✅️Step 2 — `core/logging.py`**

```
请实现 app/core/logging.py，要求：

1. 结构化 JSON 日志（生产）+ 彩色文本日志（开发），由 LOG_FORMAT 环境变量切换
2. 每条日志必须包含字段：
   timestamp, level, message, trace_id, module, function, line
3. 提供 TraceID 中间件注入（contextvars，异步安全）
4. 提供装饰器 @log_execution_time 记录函数耗时
5. 日志格式预留 ELK Filebeat 采集字段（service_name, environment, version）
6. get_logger(name) 工厂函数供其他模块调用

前置条件：settings.py 已完成。只输出 logging.py 完整代码。
```

------

**✅️Step 3 — `infrastructure/redis_client.py`**

```
请实现 app/infrastructure/redis_client.py，要求：

1. 使用 redis.asyncio 创建异步连接池（max_connections 从 settings 读取）
2. 封装以下方法（全部 async）：
   - get(key) / set(key, value, ttl) / delete(key) / exists(key)
   - get_json(key) / set_json(key, obj, ttl)（自动序列化/反序列化）
   - build_key(*parts) → 带命名空间前缀的 key 构造工具
3. 连接失败时记录日志并抛出自定义 CacheConnectionError
4. 提供 get_redis_client() 依赖注入函数（FastAPI Depends 兼容）
5. 提供健康检查方法 ping() → bool

只输出 redis_client.py，包含完整代码。
```

------

✅️**Step 4 — `core/exceptions.py` + `schemas/base.py`**

```
请同时实现以下两个文件：

【1】app/core/exceptions.py
- 定义业务异常基类 AppException(code, message, detail)
- 子类：LLMError / CacheError / VectorDBError / DocumentParseError / AgentError
- ErrorCode 枚举：对应 HTTP 状态码与业务码（如 LLM_UNAVAILABLE=50001）

【2】app/schemas/base.py
- 泛型响应模型 ApiResponse[T]：
  { success: bool, code: int, message: str, data: T, trace_id: str, timestamp: float }
- 分页模型 PageResponse[T]: data: List[T], total, page, page_size
- 空响应 EmptyResponse

这两个文件共同构成统一的错误处理与响应体系，代码量合计约 150 行，可在单次输出完整。
```

------

### ⚡ 第二阶段：FastAPI服务层（Step 5–6）

------

✅️**Step 5 — `main.py` + `api/health.py`**

```
请实现以下两个文件：

【1】main.py（FastAPI 应用入口）
- 创建 FastAPI 实例，挂载 API_PREFIX
- 注册中间件（顺序）：CORS → TraceID注入 → 请求日志 → 全局异常处理
- lifespan 生命周期：启动时初始化 Redis/Milvus 连接，关闭时释放
- 挂载所有路由（health / chat / knowledge / rag / agent）
- slowapi 速率限制（默认 60次/分钟/IP）

【2】app/api/health.py
- GET /health → { status: "ok", timestamp }
- GET /health/detailed → 并发检查 Redis ping() 和 Milvus 连通性，返回各组件状态

前置条件：Steps 1-4 已完成。
```

------

✅️**Step 6 — `infrastructure/llm_client.py` + `core/llm_router.py`**

```
请实现以下两个文件：

【1】app/infrastructure/llm_client.py
- 封装 LangChain ChatOpenAI
- async invoke(messages, model, stream=False) → str 或 AsyncGenerator
- 自动记录每次调用的 Token 消耗（prompt_tokens / completion_tokens / total）到日志
- 模型不可用时抛出 LLMError，由上层路由器捕获

【2】app/core/llm_router.py
- 路由策略枚举：QUALITY（gpt-4o） / BALANCED（gpt-4o-mini） / ECONOMY（gpt-3.5-turbo）
- route(task_type) → 返回最优模型名称
- task_type 枚举：CHAT / RAG / AGENT_REASONING / EMBEDDING
- 降级逻辑：主模型失败 → 自动切换 FALLBACK_MODEL → 再失败才抛出异常
- 每次路由决策写入 DEBUG 日志

两个文件合计约 180 行。
```

------

### 💬 第三阶段：AI聊天（Step 7）

------

**✅️Step 7 — `services/chat_service.py` + `api/chat.py`**

```
请实现以下两个文件：

【1】app/services/chat_service.py
- 维护多轮对话历史：RedisMessageHistory（key = chat:{session_id}，TTL=2h）
- async chat(session_id, user_message, model_strategy) → str
- async chat_stream(session_id, user_message) → AsyncGenerator[str]
- 使用 core/prompts.py 的聊天 PromptTemplate
- 会话历史最多保留最近 20 条消息（超出自动截断）

【2】app/api/chat.py
- POST /chat → ChatRequest(session_id, message, strategy) → ApiResponse[ChatResponse]
- POST /chat/stream → 返回 StreamingResponse（text/event-stream，SSE格式）
  SSE 格式：data: {"delta": "...", "done": false}\n\n
- 使用 FastAPI Depends 注入 chat_service

同时实现 app/schemas/chat.py（请求/响应 Pydantic 模型）。
```

------

### 🗄️ 第四阶段：RAG系统（Step 8–11）

------

✅️**Step 8 — `infrastructure/milvus_client.py`**

```
请实现 app/infrastructure/milvus_client.py，要求：

1. 异步 Milvus 连接（pymilvus），连接池管理
2. Collection 操作：
   - ensure_collection_exists(name) → 不存在则自动创建
   - Schema 定义：id(auto), content(VARCHAR 2048), 
     embedding(FLOAT_VECTOR 1536), metadata(JSON), source(VARCHAR 512), created_at(INT64)
3. 索引管理：
   - 创建 HNSW 索引（M=16, efConstruction=256），说明参数含义
   - create_index() / drop_index()
4. 健康检查：ping() → bool
5. 提供 get_milvus_client() 依赖注入函数

只输出这一个文件，约 150 行。
```

------

✅️**Step 9 — `infrastructure/embedding_client.py` + `services/vector_store.py`**

```
请实现以下两个文件：

【1】app/infrastructure/embedding_client.py
- 调用 OpenAI text-embedding-3-small 生成 Embedding（1536维）
- Redis 缓存策略：key = emb:{sha256(text)[:16]}，TTL = 24h
- async embed_text(text) → List[float]
- async embed_batch(texts) → List[List[float]]（并发，最大10个并发）
- 缓存命中率统计（hit/miss 计入日志）

【2】app/services/vector_store.py
- async upsert_documents(docs: List[Document], collection) → List[str]（返回插入的id列表）
- async similarity_search(query, collection, top_k=5, threshold=0.7) 
  → List[SearchResult(content, score, metadata, source)]
- 检索结果 Redis 缓存：key = search:{sha256(query+collection)[:16]}，TTL=10min
- async delete_by_source(source, collection)

两个文件合计约 200 行。
```

------

**✅️Step 10 — `services/document_service.py`**

```
请实现 app/services/document_service.py，要求：

1. 文档解析（使用 LangChain 文档加载器）：
   - PDF → PyPDFLoader
   - Word(.docx) → Docx2txtLoader
   - TXT / Markdown → TextLoader
   - 解析失败抛出 DocumentParseError

2. 文档分块（RecursiveCharacterTextSplitter）：
   - chunk_size=512，chunk_overlap=50
   - 保留元数据：source, page_num, chunk_index

3. 完整处理流水线（async）：
   parse_document(file_path) → split_chunks() → embed_and_store() → 返回处理报告
   { total_chunks: int, stored: int, failed: int, duration_ms: float }

4. 支持异步批量处理（asyncio.gather，并发数可配置）
5. 进度回调接口（供 WebSocket 推送使用）
```

------

**✅️Step 11 — `services/rag_service.py` + `api/rag.py` + `api/knowledge.py`**

```
请实现以下三个文件：

【1】app/services/rag_service.py
- 完整 RAG Chain（LangChain LCEL 语法）：
  query → embed → milvus_search → rerank → prompt_inject → llm_generate
- Reranking 策略：按 score 排序后取 top3，注入 Prompt
- 引用溯源：响应中附带使用了哪些文档片段（source, chunk内容前50字）
- async query(question, collection) → RAGResponse(answer, sources, tokens_used)
- async query_stream(question, collection) → AsyncGenerator[str]

【2】app/api/knowledge.py
- POST /knowledge/upload（UploadFile, collection_name）→ 触发 document_service
- GET /knowledge/list → 返回知识库中的文档列表
- DELETE /knowledge/{source_id}

【3】app/api/rag.py
- POST /rag/query → RAGQueryRequest → ApiResponse[RAGResponse]
- POST /rag/query/stream → StreamingResponse(SSE)

同时补充 app/schemas/rag.py。三个文件合计约 250 行，可适当精简实现保证完整输出。
```

------

### 🤖 第五阶段：LangGraph Agent（Step 12–15）

------

**✅️Step 12 — `agents/state.py` + `core/prompts.py`（Agent部分）**

```
请实现以下两个文件：

【1】app/agents/state.py
- AgentState(TypedDict)：
  messages, task, plan, current_step, tool_results, 
  final_answer, error, iteration_count, metadata
- TaskStatus 枚举：PENDING / RUNNING / WAITING_HUMAN / COMPLETED / FAILED
- AgentMessage 模型：role(system/user/assistant/tool), content, tool_name, timestamp
- 定义 Agent 间通信的标准消息格式（用于多Agent协作）

【2】app/core/prompts.py（补充 Agent 提示词部分，之前只有 Chat/RAG）
- AGENT_SYSTEM_PROMPT（通用Agent系统提示词）
- PLANNER_PROMPT（任务规划）
- SUPERVISOR_PROMPT（Supervisor分解任务）
- WORKER_PROMPTS dict：{ "researcher": ..., "writer": ..., "reviewer": ... }
```

------

**✅️Step 13 — `agents/tools/`（4个工具文件）**

```
请实现 agents/tools/ 目录下的4个工具文件：

【1】web_search.py
- 使用 DuckDuckGo Search（duckduckgo-search 库）或 Tavily API
- @tool 装饰器，输入 query:str，返回 List[SearchResult]
- 限制最多返回5条结果，包含 title/url/snippet

【2】calculator.py
- @tool 装饰器，安全执行数学表达式（使用 numexpr 或 ast.literal_eval）
- 禁止执行任意代码，只允许数学运算
- 返回计算结果或错误信息

【3】rag_retrieval.py
- @tool 装饰器，调用内部 vector_store 检索知识库
- 输入：query:str, collection:str="default", top_k:int=3
- 将 vector_store.similarity_search 包装为 Agent 可调用的工具

【4】code_executor.py
- @tool 装饰器，在受限沙箱中执行 Python 代码（使用 RestrictedPython）
- 超时限制 10 秒，禁止网络/文件系统访问
- 返回 stdout 输出或异常信息

4个文件合计约 200 行。
```

------

*✅️Step 14 — `agents/workflow.py`（LangGraph 单Agent状态机）**

```
请实现 app/agents/workflow.py，要求：

1. 使用 LangGraph StateGraph 构建完整状态机：
   节点：planner → tool_selector → tool_executor → observer → [结束或循环]

2. 节点实现：
   - planner_node：分析任务，生成执行计划
   - tool_selector_node：根据计划选择合适工具
   - tool_executor_node：调用工具，记录结果
   - observer_node：判断是否完成任务，决定下一步
   - human_interrupt_node：等待人工确认（条件触发）

3. 条件边逻辑：
   observer → END（任务完成）
   observer → planner（需要继续规划）
   observer → human_interrupt（需要人工确认）
   最大迭代次数保护（默认10次，超出强制结束）

4. 状态持久化：使用 MemorySaver（可替换为 Redis 持久化）
5. 提供 async run_workflow(task, session_id) → AgentState 入口函数

输出完整代码，并用注释说明状态机流转图。约 220 行。
```

------

**✅️Step 15 — `agents/supervisor.py` + `agents/workers.py`**

```
请实现多Agent协作系统：

【1】app/agents/supervisor.py
- Supervisor Agent（基于 LangGraph）：
  接收用户任务 → 分解为子任务列表 → 分配给对应 Worker
  收集所有 Worker 结果 → 整合最终输出
- 任务分配策略：
  信息收集类 → ResearchAgent
  内容生成类 → WriterAgent  
  质量检查类 → ReviewerAgent
- 支持串行和并行两种分配模式（通过 parallel:bool 参数控制）

【2】app/agents/workers.py
- ResearchAgent：调用 web_search + rag_retrieval 工具，返回结构化研究报告
- WriterAgent：根据研究内容生成指定格式的文档
- ReviewerAgent：审核内容质量，返回评分（1-10）和修改建议
- 每个 Worker 是独立的 LangGraph 子图，可独立运行

两个文件合计约 250 行，如超出请优先保证 supervisor.py 完整。
```

------

### 🔌 第六阶段：Agent API + WebSocket（Step 16）

------

**✅️Step 16 — `api/agent.py` + `schemas/agent.py`**

```
请实现以下两个文件：

【1】app/schemas/agent.py
- AgentRunRequest: task(str), mode(single/multi), session_id, config(dict)
- AgentRunResponse: task_id, status, result, steps, tokens_used, duration_ms
- AgentStepEvent: step_type, node_name, content, timestamp（WebSocket推送用）

【2】app/api/agent.py
- POST /agent/run → 异步启动 Agent 任务，立即返回 task_id
  （任务在后台 asyncio.create_task 中运行，结果存 Redis）
- GET /agent/status/{task_id} → 查询任务当前状态和结果
- POST /agent/cancel/{task_id} → 取消运行中的任务
- WebSocket /agent/stream/{task_id}：
  客户端连接后，实时推送 AgentStepEvent（每个节点执行时推送一条）
  任务完成时推送 { type: "done", result: ... }

重点说明 WebSocket 实时推送与后台异步任务的协调方式（使用 Redis Pub/Sub）。
```

------

### 🐳 第七阶段：部署 + 测试（Step 17–18）

------

**✅️Step 17 — Docker 完整部署配置**

```
请提供完整的 Docker 部署配置，包含以下所有文件：

【1】Dockerfile
- 基于 python:3.11-slim，多阶段构建（builder安装依赖 + runtime运行）
- 非 root 用户（uid=1000），WORKDIR=/app
- HEALTHCHECK CMD curl -f http://localhost:8000/health

【2】docker-compose.yml（开发环境）
服务清单（含端口和依赖关系）：
- app: FastAPI 主服务，端口 8000，depends_on: redis, milvus
- redis: redis:7-alpine，开启 AOF 持久化，端口 6379
- milvus: milvusdb/milvus:v2.4.0，依赖 etcd + minio
- etcd: bitnami/etcd（Milvus元数据存储）
- minio: minio/minio（Milvus对象存储）
- nginx: 反向代理，端口 80/443

【3】docker-compose.prod.yml（生产覆盖）
- app 副本数 2，resource limits(CPU:1, Memory:2G)
- 所有服务的 restart:always

【4】scripts/init_db.sh
- 等待 Milvus 就绪 → 自动创建 Collection → 创建 HNSW 索引

每个文件单独用代码块输出，包含注释说明。
```

------

**Step 18 — 集成测试 + README**

```
请实现以下内容：

【1】tests/test_chat_api.py
- 使用 pytest + httpx.AsyncClient + pytest-asyncio
- 测试用例：普通对话、流式对话、多轮对话记忆、无效请求验证

【2】tests/test_rag_pipeline.py
- 上传测试文档 → 等待处理 → 发起 RAG 查询 → 断言返回引用溯源
- Mock Milvus 和 OpenAI（避免真实API调用）

【3】tests/test_agent_workflow.py
- 单 Agent 任务运行、状态轮询、WebSocket 推送消息格式验证
- Mock 所有工具调用

【4】README.md（精简版）
- 系统架构一句话描述
- 快速启动（3个命令跑通）：git clone → cp .env.example .env → docker-compose up
- 环境变量必填项说明表（5个核心变量）
- 核心接口速查表（10个接口，一行一个）

测试文件合计约 200 行，README 约 80 行。
```

------

### **✅️Step 19 — `core/auth.py`（Auth认证体系）**

```
请实现 app/core/auth.py，为系统添加完整认证体系：

1. 双认证模式：
   - JWT Token 认证（Bearer Token，有效期 24h，刷新机制）
   - API Key 认证（X-API-Key Header，存储于 PostgreSQL，支持按Key限速）

2. FastAPI 依赖注入：
   - get_current_user(token) → User（JWT路径）
   - get_api_key_user(api_key) → User（API Key路径）
   - optional_auth()：允许匿名访问的可选认证

3. 权限装饰器：
   - @require_role("admin" / "user" / "readonly")
   - 在路由函数上直接使用

4. API Key 管理：
   - 生成：随机 32 字节 hex，bcrypt 哈希后存储，明文只显示一次
   - 支持 Key 的启用/禁用/过期时间设置

5. 安全要求：
   - JWT 密钥从 settings 读取，支持密钥轮转
   - 所有认证失败统一返回 401，不泄露失败原因细节

前置条件：settings.py（Step 1）、postgres_client.py（Step 21）已完成。
```

------

### **✅️Step 20 — `infrastructure/task_queue.py` + `workers/`（任务队列）**



```
请实现 Celery 异步任务队列系统：

【1】app/infrastructure/task_queue.py
- Celery 应用配置（broker=Redis，backend=Redis）
- 任务队列定义（3个优先级队列）：
  - high_priority：Agent实时任务
  - default：文档处理任务
  - low_priority：评估、统计任务
- 通用任务装饰器：@celery_task（自动记录日志 + 异常捕获 + 重试3次）
- 提供 get_task_status(task_id) → TaskResult 查询函数

【2】app/workers/document_worker.py
- @celery_task：process_document(file_path, collection_name, user_id)
  调用 document_service 完整处理流程
  完成后更新 PostgreSQL 任务状态表
  支持进度报告（0-100%）存入 Redis

【3】app/workers/agent_worker.py
- @celery_task：run_agent_task(task_id, task_config, session_id)
  调用 agents/workflow.py 或 agents/supervisor.py
  每个节点执行后通过 Redis Pub/Sub 发布状态更新
  最终结果写入 PostgreSQL task 表

重点：说明 Celery Worker 与 FastAPI 主进程通过 Redis Pub/Sub 的协作机制。
```

------

### **✅️Step 21 — `infrastructure/postgres_client.py` + `models/`（结构化数据库）**



```
请实现 PostgreSQL 异步数据库层：

【1】app/infrastructure/postgres_client.py
- 使用 asyncpg + SQLAlchemy 2.0（async_sessionmaker）
- 连接池配置（pool_size=10, max_overflow=20）
- 提供 get_db_session() 异步上下文管理器（FastAPI Depends 兼容）
- 数据库迁移：集成 Alembic，提供初始 migration 文件

【2】app/models/base.py
- DeclarativeBase 基类
- 公共字段：id(UUID), created_at, updated_at, is_deleted（软删除）

【3】app/models/user.py
- 字段：id, username, email, hashed_password, role, api_keys(关联), is_active
- 关联：一用户对应多个 API Key

【4】app/models/task.py
- 字段：id, user_id, task_type, status, input_data(JSONB), result(JSONB),
  celery_task_id, progress(0-100), error_message, started_at, finished_at

【5】app/models/prompt_version.py
- 字段：id, name, version(semver), content, variables(JSONB),
  is_active, ab_test_weight, created_by, tags

注意：所有模型配合 Alembic autogenerate 自动生成迁移脚本。
```

------

### **✅️Step 22 — `agents/tool_registry.py` + 新工具（工具注册中心）**



```
请实现动态工具注册系统与新工具：

【1】✅️ app/agents/tool_registry.py
- ToolRegistry 单例类：
  - register(tool, roles=None)：注册工具，支持角色权限过滤
  - get_tools_for_agent(agent_type, user_role) → List[BaseTool]
  - get_tool_schema() → OpenAPI格式的工具Schema列表
  - 工具调用审计：每次调用记录到日志（tool_name, input, output, latency_ms）
- 预定义工具组：
  - RESEARCH_TOOLS = [web_search, rag_retrieval, db_query]
  - EXECUTION_TOOLS = [calculator, code_executor, external_api]
  - ALL_TOOLS = RESEARCH_TOOLS + EXECUTION_TOOLS

【2】✅️ app/agents/tools/db_query.py
- @tool：query_database(sql: str, database: str="default") → str
- 只允许 SELECT 语句（AST解析校验，禁止 DDL/DML）
- 查询结果最多返回 100 行，自动格式化为 Markdown 表格
- 超时限制：5 秒

【3】✅️app/agents/tools/external_api.py
- @tool：call_external_api(url, method, headers_json, body_json) → str
- 白名单域名校验（从 settings 读取允许的域名列表）
- 超时：10秒，最大响应 1MB
- 自动解析 JSON 响应，错误时返回结构化错误信息

说明：Tool Registry 如何支持不同 Agent 使用不同工具子集。
```

------

### **Step 23 — `services/memory_service.py`（四种记忆系统）**



```
请实现完整的 Memory System，管理4种记忆类型：

app/services/memory_service.py

【记忆类型1】Short-term Memory（短期对话记忆）
- 存储：Redis，key=mem:short:{session_id}，TTL=2h
- 内容：最近20条消息列表（role + content + timestamp）
- async get_recent(session_id, n=10) → List[Message]
- async append(session_id, message) → None（超20条自动截断）

【记忆类型2】Long-term Memory（长期向量记忆）
- 存储：Milvus（独立 Collection: long_term_memory）
- 触发：对话结束时，自动将重要信息摘要后存入向量
- async store_memory(user_id, content, memory_type)
- async recall(user_id, query, top_k=3) → List[MemoryItem]
- 摘要策略：超过10轮对话，用LLM自动生成摘要存储

【记忆类型3】User Profile Memory（用户画像记忆）
- 存储：PostgreSQL（users表扩展字段 preferences JSONB）
- 内容：用户偏好、使用习惯、常用语言、领域标签
- async get_profile(user_id) → UserProfile
- async update_profile(user_id, updates: dict) → None
- 自动从对话中提取偏好信息（每10次对话触发一次）

【记忆类型4】Task Memory（任务上下文记忆）
- 存储：Redis（key=mem:task:{task_id}）+ PostgreSQL持久化
- 内容：任务执行过程中各节点的中间结果、工具调用历史
- async save_task_context(task_id, node_name, context) → None
- async get_task_context(task_id) → TaskContext

提供统一入口 MemoryManager 类，聚合4种记忆的读写操作。
```

------

### **Step 24 — `core/observability.py` + `services/cost_service.py`（可观测性）**



```
请实现可观测性与成本监控系统：

【1】app/core/observability.py
- Prometheus 指标定义（使用 prometheus-client）：
  - http_request_duration_seconds（Histogram，P50/P95/P99）
  - llm_token_usage_total（Counter，labels: model, user_id, endpoint）
  - cache_hit_ratio（Gauge，labels: cache_type）
  - agent_task_duration_seconds（Histogram，labels: task_type）
  - active_connections（Gauge）
- 指标采集中间件（自动为每个请求记录延迟和状态码）
- 暴露 GET /metrics 端点（供 Prometheus 抓取）
- 提供 track_llm_call(model, prompt_tokens, completion_tokens) 工具函数

【2】app/services/cost_service.py
- 模型单价配置（$/1K tokens，从 settings 读取或硬编码）：
  gpt-4o: $0.005/input, $0.015/output
  gpt-4o-mini: $0.00015/input, $0.0006/output
  text-embedding-3-small: $0.00002/1K tokens
- async record_usage(user_id, model, tokens, endpoint) → None（写入PostgreSQL）
- async get_user_cost(user_id, start_date, end_date) → CostReport
- async get_cost_by_model(start_date, end_date) → Dict[model, cost]
- 日费用预警：超过阈值时触发告警日志（可扩展为邮件/Slack通知）

两个文件合计约 200 行。
```

------

### **Step 25 — `services/prompt_version_service.py` + `services/eval_service.py`**



```
请实现 Prompt 版本管理与 LLM 评估系统：

【1】app/services/prompt_version_service.py
- Prompt 版本管理（基于 PostgreSQL prompt_version 表）：
  - async create_version(name, content, variables) → PromptVersion
  - async get_active(name) → PromptVersion（获取当前生效版本）
  - async rollback(name, version) → None（回滚到指定版本）
  - A/B 测试：按 ab_test_weight 权重随机选择版本
    get_ab_version(name) → PromptVersion（50/50 或自定义权重）
  - 变量渲染：render(template_content, variables_dict) → str

【2】app/services/eval_service.py
- LLM 评估 Pipeline（用于 RAG 和 Agent 输出质量评估）：

  评估指标：
  - async eval_relevance(question, answer) → float (0-1)
    判断回答是否与问题相关
  - async eval_faithfulness(context, answer) → float (0-1)
    判断回答是否忠实于检索到的上下文（幻觉检测）
  - async eval_answer_quality(question, answer, ground_truth=None) → EvalScore
    综合质量评分（包含 BLEU/ROUGE 或 LLM-as-judge）

  评估方式：LLM-as-judge（让 gpt-4o-mini 对输出打分，返回 JSON 格式评分）

  - async run_regression_eval(test_dataset: List[EvalCase]) → EvalReport
    批量跑评估集，输出整体准确率、P95延迟、平均幻觉率

  评估结果持久化到 PostgreSQL，支持查看历史趋势。

两个文件合计约 220 行。
```

------

### **Step 26 — 更新 Docker 配置（补充 PostgreSQL + Celery + MinIO）**



```
请在原 docker-compose.yml 基础上补充新增服务，输出完整的最终版本：

新增服务：

【postgres】
- 镜像：postgres:16-alpine
- 环境变量：POSTGRES_DB, POSTGRES_USER, POSTGRES_PASSWORD
- 数据卷持久化，端口 5432
- 健康检查：pg_isready

【celery_worker】
- 基于同一个 Dockerfile（app镜像）
- 启动命令：celery -A app.infrastructure.task_queue worker
  --loglevel=info --concurrency=4 -Q high_priority,default,low_priority
- depends_on: app, redis, postgres
- 资源限制：CPU=2, Memory=2G

【celery_flower】（可选监控）
- 镜像：mher/flower
- Celery 任务监控仪表盘，端口 5555

【minio】（文档对象存储）
- 已在 Milvus 依赖中存在，但需暴露 API 端口 9000 给 app 使用
- 补充 MINIO_ROOT_USER / MINIO_ROOT_PASSWORD 环境变量

同时更新：
- .env.example（补充 POSTGRES_URL, CELERY_BROKER_URL 等新变量）
- scripts/init_db.sh（补充：运行 Alembic migrate + 创建默认管理员账户）
- Dockerfile（补充安装 asyncpg, celery, prometheus-client 等新依赖）
```

------

## 🔄 原有步骤需要补充的说明

| 原步骤                         | 需补充内容                                                   | 补充方式                                          |
| ------------------------------ | ------------------------------------------------------------ | ------------------------------------------------- |
| **Step 3**（redis_client.py）  | 添加 `publish(channel, message)` 和 `subscribe(channel)` 方法，用于 Message Broker | 在执行Step 3时加入 Pub/Sub 方法                   |
| **Step 5**（main.py）          | 挂载 Auth 中间件（依赖 Step 19），注册 `/metrics` 端点（依赖 Step 24） | Step 5 骨架先留占位，Step 19/24 完成后补充        |
| **Step 12**（agents/state.py） | AgentState 增加 `memory_context: MemoryContext` 字段，用于承载 Step 23 注入的记忆内容 | 执行 Step 12 时预留字段，Step 23 完成后填充       |
| **Step 16**（api/agent.py）    | POST /agent/run 改为提交 Celery 任务（而非 asyncio.create_task），依赖 Step 20 | Step 16 先用 asyncio，Step 20 完成后替换为 Celery |

------

## ✅ 最终完整执行路线图（26步）



```
Step  1- 4   基础设施骨架     → 配置/日志/Redis/异常/响应体
Step  5- 6   FastAPI服务层    → 入口/健康检查/LLM路由
Step  7      AI聊天           → 流式对话可运行 ✅
Step  8-11   RAG系统          → 知识库问答可运行 ✅
Step 12-14   单Agent工作流    → LangGraph任务执行可运行 ✅
Step 15-16   多Agent协作       → Supervisor + WebSocket推送 ✅
──────────────────────────── 以上为原18步 ────────────────────────────
Step 19      Auth认证         → JWT + API Key 生产级安全
Step 20      任务队列         → Celery 异步解耦架构
Step 21      PostgreSQL       → 结构化数据持久化
Step 22      工具注册中心      → 动态扩展工具体系
Step 23      记忆系统         → 4种记忆类型全覆盖 ✅
Step 24      可观测性         → Prometheus + 成本监控
Step 25      评估体系         → Prompt版本 + LLM质量评估
Step 26      部署更新         → Docker完整编排（含新服务）✅
```

> 💡 **建议**：Step 19-21 是后续步骤的**基础依赖**，必须先完成。Step 22-25 可以相对独立并行推进。整个系统26步完成后，对应你架构图的9个层全部落地。